group=io.github.chasehuegel
version=1.0-SNAPSHOT
# Configuration cache disabled due to buildFrontend Exec task (not yet seralizable)
# Re-enable once the frontend build pipeline is finalized
org.gradle.configuration-cache=false
org.gradle.parallel=true
org.gradle.caching=true
